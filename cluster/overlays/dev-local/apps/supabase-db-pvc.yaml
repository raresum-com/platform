apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: supabase-supabase-db-pvc
  namespace: supabase
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path  # Explicitly set to match existing bound PVC
  resources:
    requests:
      storage: 8Gi
